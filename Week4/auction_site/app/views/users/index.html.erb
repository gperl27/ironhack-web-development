<h1>Users#index</h1>

<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.name, "/users/#{user.id}" %></li>
    <%= link_to 'Destroy', "/users/#{user.id}", 
        method: :delete, 
        data: { confirm: "You sure?" } %>
  <% end %>
</ul>
